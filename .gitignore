# ------------ General Node/JS ------------
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
coverage/
.tmp/
temp/
tmp/
.cache/
.eslintcache

# Lock files — keep EXACTLY ONE committed for reproducible installs.
# If você usa npm, mantenha package-lock.json e REMOVA os outros.
# Se usa yarn/pnpm, mantenha o lock correspondente e remova os demais.
# (Por padrão, não ignoramos nenhum aqui.)
# yarn.lock
# pnpm-lock.yaml

# ------------ Expo / React Native ------------
.expo/
.expo-shared/
dist/
web-build/

# ------------ Android / Gradle (outputs locais) ------------
.gradle/
android/.gradle/
android/build/
android/**/build/
android/captures/
*.iml
local.properties     
# contém o caminho do SDK local — NÃO versione

# ------------ Keystores / Assinaturas / Certificados (NÃO versione) ------------
*.keystore
*.jks
keystore.properties
*.keystore.properties
*.p12
*.p8
*.pem
*.cer
*.csr

# ------------ Ambientes / Segredos ------------
.env
.env.*
!.env.example        
# mantenha um modelo sem segredos

# ------------ SO / Editor ------------
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

expo-env.d.ts